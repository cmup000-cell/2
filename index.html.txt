<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Моё меню</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Nunito&display=swap');

    html, body {
      margin: 0;
      padding: 0;
      background: none !important;
      font-family: 'Nunito', sans-serif;
    }

    @keyframes gradientMove {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .wrapper {
      background: linear-gradient(135deg, #1c1c1c, #444444, #1c1c1c);
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background-size: 400% 400%;
      animation: gradientMove 40s ease infinite;
      flex-direction: column;
    }

    /* Большая кнопка, уменьшается при открытии */
    .toggle-button {
      background-color: #181818;
      color: #999;
      font-size: 22px;
      padding: 20px 60px;
      border-radius: 8px;
      cursor: pointer;
      border: none;
      transition: all 0.4s ease;
      margin-bottom: 15px;
    }
    .toggle-button.active {
      font-size: 20px;
      padding: 12px 40px;
      border-radius: 6px;
    }

    /* Скрытый блок */
    .content {
      max-height: 0;
      overflow: hidden;
      opacity: 0;
      transition: max-height 0.5s ease, opacity 0.5s ease;
      background-color: #222;
      border-radius: 5px;
      padding: 0;
      width: 260px;
    }
    .content.show {
      max-height: 400px;
      opacity: 1;
      padding: 15px;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    /* Растянутые ссылки как кнопки */
    .my-button {
      display: block;
      width: 100%;
      text-align: center;
      text-decoration: none;
      padding: 12px 0;
      background-color: #181818;
      color: #999;
      font-family: 'Nunito', sans-serif;
      font-size: 18px;
      border-radius: 5px;
      transition: background-color 0.3s, transform 0.2s;
      cursor: pointer;
    }
    .my-button:hover {
      background-color: #0b0b0b;
      transform: scale(1.02);
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <button class="toggle-button">Меню</button>
    <div class="content">
      <a href="https://sites.google.com/view/mrac-18/Main/12" class="my-button" target="_self">Кнопка 1</a>
      <a href="https://example.com/page2" class="my-button" target="_self">Кнопка 2</a>
      <a href="https://example.com/page3" class="my-button" target="_self">Кнопка 3</a>
      <a href="https://example.com/page4" class="my-button" target="_self">Кнопка 4</a>
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const toggleBtn = document.querySelector('.toggle-button');
      const content = document.querySelector('.content');

      toggleBtn.addEventListener('click', () => {
        content.classList.toggle('show');
        toggleBtn.classList.toggle('active');
      });
    });
  </script>
</body>
</html>
